apiVersion: integrations.k8smgmt.io/v3
kind: SecretStore
metadata:
  name: secrets12
  project: rakshith-ws
spec:
  config:
    vault:
      clusters:
      - authPath: test21
        clusterName: rakshith-prod-eks-cluster
      host: https://www.example.com/port
  provider: Vault
