apiVersion: security.k8smgmt.io/v3
kind: ClusterNetworkPolicyRule
metadata:
  name: cluster-scoped-rule
  project: giri-test
spec:
  artifact:
    artifact:
      paths:
      - name: file://artifacts/cluster-scoped-rule/cluster-wide.yaml
    options: {}
    type: Yaml
  version: v2
