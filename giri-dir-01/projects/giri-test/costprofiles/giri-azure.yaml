apiVersion: cost.k8smgmt.io/v3
kind: CostProfile
metadata:
  name: giri-azure
  project: giri-test
spec:
  installationParams:
    azure:
      customPricing:
        cloudCredentialsName: giri-aks
  providerType: azure
  version: v1
