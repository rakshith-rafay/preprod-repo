apiVersion: integrations.k8smgmt.io/v3
kind: Repository
metadata:
  name: giri-parameters
  project: giri-test
spec:
  credentials:
    password: sealed://credentials.password
    username: giri-rafay
  endpoint: https://github.com/giri-rafay/parameters.git
  secret:
    name: file://artifacts/giri-parameters/sealed-secret.yaml
  type: Git
